{{ mojPrimaryNavigation({
  label: 'Primary navigation',
  items: [
    {
      text: 'Your applications',
      href: '/' + version + '/provider/crm-apps-reviewed',
      active: + activePrimeNav[0] | default('true')
    },
    {
      text: 'Search',
      href: '/' + version + '/provider/crm-apps-search',
       active: + activePrimeNav[1]
    }
  ]
}) }}