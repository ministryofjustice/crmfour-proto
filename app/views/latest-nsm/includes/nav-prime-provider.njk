{{ mojPrimaryNavigation({
  label: 'Primary navigation',
  items: [
    {
      text: 'Your claims',
      href: '/' + version + '/provider/crm-claims-reviewed',
      active: + activePrimeNav[0] | default('true')
    },
    {
      text: 'Search',
      href: '/' + version + '/provider/crm-claims-search',
      active: + activePrimeNav[1]
    }
  ]
}) }}